"""Internal implementation modules for the AI analysis task.

Imports stay lazy so the web application can start and let the user install the
optional voice encoder only when they actually run AI 查找.
"""
